# Cursor

Use the OpenAI-compatible custom provider path.

- Base URL: `https://api.ai-router.dev/v1`
- API key: your AI Router key
- Model: `gpt-4.1-mini` or another model returned by `/v1/models`

Quick checks:

- If the endpoint test fails, verify the URL includes `/v1`.
- If model discovery is incomplete, enter the model id manually.
