# Open WebUI

- API URL: `https://api.ai-router.dev/v1`
- API key: your AI Router key
- Model: choose one returned by `/v1/models`

Notes:

- If the UI tests the root URL instead of the API URL, it will fail. Use the API host with `/v1`.
- If model discovery is partial, enter a model id manually.
